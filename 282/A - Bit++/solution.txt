package mainimport "fmt"  func main() {	        var n,x int	    fmt.Scan(&n)    for i := 1; i <= n; i++ {        var s string        fmt.Scan(&s)        if s[1] == '+' {            x++        } else {            x--        }    }    fmt.Print(x)}